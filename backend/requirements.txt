# Core
manim
numpy
scipy

# Backend
fastapi
uvicorn[standard]

# AI Integration
openai
OpenAI  # if you're using openai.OpenAI (separate SDK from openai.ChatCompletion)
requests
python-dotenv

# Security & Auth
python-jose[cryptography]
itsdangerous
authlib  # used for OAuth

# Database
pymongo

# Cloudinary Upload
cloudinary

# Utility & Typing
typing  # not strictly needed in Python 3.5+, but harmless
bson  # for ObjectId usage
python-multipart  # if you're dealing with form-data uploads (FastAPI requires it for file uploads)

# Sessions
starlette
